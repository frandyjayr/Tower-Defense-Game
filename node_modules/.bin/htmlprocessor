#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../htmlprocessor/bin/htmlprocessor" "$@"
  ret=$?
else 
  node  "$basedir/../htmlprocessor/bin/htmlprocessor" "$@"
  ret=$?
fi
exit $ret
