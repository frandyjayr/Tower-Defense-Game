#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../github-releases/bin/github-releases" "$@"
  ret=$?
else 
  node  "$basedir/../github-releases/bin/github-releases" "$@"
  ret=$?
fi
exit $ret
